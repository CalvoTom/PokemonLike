<Window x:Class="PokemonLike.Views.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Login" Height="350" Width="400">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Text="Login" FontSize="24" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center" />
        <TextBox Grid.Row="1" x:Name="UsernameTextBox" VerticalAlignment="Center" Margin="10" />
        <PasswordBox Grid.Row="2" x:Name="PasswordBox" VerticalAlignment="Center" Margin="10" />
        <Button Grid.Row="3" Content="Login"  HorizontalAlignment="Center" Click="LoginButton_Click" Margin="0,0,0,54" Style="{StaticResource PokemonButtonStyle}" Width="128" />
        <Button Grid.Row="3" Content="Signup"  HorizontalAlignment="Center" Click="SignupButton_Click" Margin="0,29,0,24" Style="{StaticResource PokemonButtonStyle}" Width="128" />
    </Grid>
</Window>
